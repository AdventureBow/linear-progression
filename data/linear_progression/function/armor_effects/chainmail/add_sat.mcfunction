# @s = player — sat drip pulse.
# Vanilla cannot write foodSaturationLevel. The saturation effect (1 gametick,
# amp 0) grants +2 saturation (and +1 food if hunger is not full).

effect give @s minecraft:saturation 1 0 true
